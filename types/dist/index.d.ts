declare function calculateTax(amount: number): number;
declare function writePrice(product: string, price: number): void;
declare const hat: [string, number, number?];
declare const gloves: [string, number, number?];
declare const items: [string, number, (number | undefined)?][];
